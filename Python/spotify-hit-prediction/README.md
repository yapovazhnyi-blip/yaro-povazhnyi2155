# 🎵 Spotify Hit Prediction

Predicts whether a song becomes a hit using audio features from Spotify.

## Setup
pip install -r requirements.txt

## Run training
python train.py

## Run app
streamlit run app.py
